# 同伴审查意见与处理记录

- 被审查作品：迷你版校园公共信息与数据展示中心（`integration/`）
- 审查方式：同伴本地运行 `python -m http.server 8787` 后现场操作 + 阅读 `js/app.js`、`three-d/scene.js`
- 审查轮次：1 轮
- 审查同伴：【请填写同伴姓名/学号】　审查日期：2026-09-19
- 处理人：本人（20251060233）　回复日期：2026-09-19

## 一、意见逐条处理表

| # | 同伴意见 | 结论 | 处理说明 | 落地位置 / 证据 |
| --- | --- | --- | --- | --- |
| 1 | 筛选条件选过之后只能再手动一个个选回"全部楼层/全部状态"，应提供一键重置 | **采纳** | 筛选条新增「重置筛选」按钮，点击后两个下拉恢复 `all` 并立即重新渲染 | `index.html` 筛选条按钮；`js/app.js` `resetFilter` 事件；截图 01/05 可见按钮 |
| 2 | 把 `data.json` 改成空数组测试时，图表区什么都不显示，分不清是"没数据"还是"出 bug 了" | **采纳** | 空数据与断网分开处理：空数据显示琥珀色中性提示「本月暂无自习室使用量数据……」并清空旧图；断网仍为红色错误条。新增 `?data=` 参数与 `data/data.empty.json` 专门用于空数据自测 | `js/app.js` `loadData()` 空数组分支；`data/data.empty.json`；截图 `07-empty-data.png` |
| 3 | 三维页如果遇到不支持 WebGL 的浏览器（或关闭硬件加速），整页白屏，用户不知道发生了什么 | **采纳** | 场景初始化前用 canvas 探测 WebGL 支持性，不支持时显示降级提示卡片（含原因说明与返回首页按钮）并安全退出，不再继续执行渲染代码 | `three-d/scene.html` `#webglFallback`；`three-d/scene.js` `webglAvailable()` 检测 |
| 4 | 手机宽度下首页大标题字号偏大，标题被折成两三行占掉近一屏 | **采纳** | ≤575.98px 断点下横幅标题收敛到 1.45rem、副标题 0.95rem，一行半内显示完整 | `css/style.css` 手机端媒体查询；截图 `03-width-mobile-375.png` |
| 5 | 建议把三维场景直接嵌在首页里滚动播放，这样更"炫"，不用多点一次 | **拒绝** | 首页常驻 WebGL 画布会持续占用 GPU/电量、拖慢低端机首屏，且三维交互（拖拽/滚轮）与页面滚动会冲突；保持"入口卡片 + 独立场景页"既符合课堂案例的模块边界，也对手机端更友好。该诉求改以正式版计划中的"首屏缩略动图/轻量海报"方式满足 | 见 [final-project-plan.md](final-project-plan.md) 风险 R4 与正式版任务 T6 |
| 6 | 首页图表和自习室列表名字一致，但操作上没有联动，建议点击/筛选自习室时图表同步高亮 | **修改（推迟到正式版）** | 方向正确，但原型目标是验证四模块整合骨架，联动需要统一的数据适配层与 ECharts 高亮状态管理，工作量超出原型范围；已拆为正式版任务 T4，并保留当前"同名数据"这一衔接基础 | 正式版任务 T4（见实施计划） |

## 二、处理结果核对

- 采纳 4 条（#1~#4）：均已改代码并重新截图验证，三档宽度/空数据/Console 自查全部重跑通过。
- 拒绝 1 条（#5）：已向同伴说明性能与交互冲突理由，对方认可，替代方案进入正式版计划。
- 修改/推迟 1 条（#6）：已写入正式版实施计划任务 T4，并注明原型已有的数据衔接基础。
- 回归情况：上述改动后重新执行质量清单 7 项检查，无新增问题（见 [quality-checklist.md](quality-checklist.md)）。
